import { DisplaySettingPipe } from './display-setting.pipe';

describe('DisplaySettingPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplaySettingPipe();
    expect(pipe).toBeTruthy();
  });
});
