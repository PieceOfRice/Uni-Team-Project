import { DisplayGamesPerMatchPipe } from './display-games-per-match.pipe';

describe('DisplayGamesPerMatchPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayGamesPerMatchPipe();
    expect(pipe).toBeTruthy();
  });
});
