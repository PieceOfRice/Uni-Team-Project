import { OrganiserNamePipe } from './organiser-name.pipe';

describe('OrganiserNamePipe', () => {
  it('create an instance', () => {
    const pipe = new OrganiserNamePipe();
    expect(pipe).toBeTruthy();
  });
});
