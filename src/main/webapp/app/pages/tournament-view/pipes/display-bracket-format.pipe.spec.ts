import { DisplayBracketFormatPipe } from './display-bracket-format.pipe';

describe('DisplayBracketFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayBracketFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
