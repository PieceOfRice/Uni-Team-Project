import { DisplayAccessStatusPipe } from './display-access-status.pipe';

describe('DisplayAccessStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayAccessStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
