import { DisplayParticipantsPipe } from './display-participants.pipe';

describe('DisplayParticipantsPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayParticipantsPipe();
    expect(pipe).toBeTruthy();
  });
});
